---
phase: 03-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LayoutAvaliacaoBuilder.ts
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "Every public method on LayoutAvaliacaoBuilder has a JSDoc comment above it stating what it does"
    - "Every JSDoc comment states what value(s) are valid for the parameter"
    - "Every JSDoc comment states whether calling the method is optional or required before build()"
    - "build() JSDoc describes the return value shape and the AssessmentInput parameter"
  artifacts:
    - path: "src/LayoutAvaliacaoBuilder.ts"
      provides: "JSDoc-documented builder class"
      contains: "@param"
  key_links:
    - from: "JSDoc on marcaDaguaRascunho"
      to: "src/LayoutAvaliacaoBuilder.ts marcaDaguaRascunho method"
      via: "JSDoc comment directly above method definition"
      pattern: "marcaDaguaRascunho"
    - from: "JSDoc on build()"
      to: "src/LayoutAvaliacaoBuilder.ts build() method"
      via: "JSDoc comment directly above build() method definition"
      pattern: "build"
---

<objective>
Add JSDoc comments to every public method of LayoutAvaliacaoBuilder (DOCS-03). Each comment must state what the method does, what values are valid for its parameter(s), and whether the method is optional before calling build().

Purpose: A developer using an IDE (VS Code, WebStorm) gets inline documentation when hovering over builder method calls. No source code reading required.

Output: src/LayoutAvaliacaoBuilder.ts with JSDoc on all 16 public methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/LayoutAvaliacaoBuilder.ts
@src/types/AssessmentInput.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSDoc to all public LayoutAvaliacaoBuilder methods (DOCS-03)</name>
  <files>src/LayoutAvaliacaoBuilder.ts</files>
  <read_first>
    - src/LayoutAvaliacaoBuilder.ts — read the full file before touching anything; every method signature, parameter name, parameter type, and return type must match exactly what is already in the file. The JSDoc comments are ADDED above existing method definitions — method bodies are NOT changed.
    - src/types/AssessmentInput.ts — read to understand the AssessmentInput and AssessmentLayoutInput types so the JSDoc on build() accurately describes the input parameter.
  </read_first>
  <action>
    Add a JSDoc block comment (/** ... */) directly above each of the following public method definitions in src/LayoutAvaliacaoBuilder.ts. Do not modify any method signature, parameter name, or method body. Do not add JSDoc to private fields or the constructor. Add JSDoc only to public methods.

    The JSDoc block for each method must include:
    - A @description line (or leading description sentence) stating what the method does in one sentence
    - A @param tag for each parameter with its type and a statement of valid values or accepted range
    - A @returns tag stating `{LayoutAvaliacaoBuilder}` with the text "Returns the builder instance for chaining" (all methods except build() return `this`)
    - An @optional or inline note in the description stating "Optional — if not called, [default behavior]"

    Exact JSDoc content for each method (add these above the corresponding method definition):

    habilitarMarcaDaguaRascunho(enabled: boolean):
    - Description: "Enables or disables the draft watermark overlay on every page."
    - @param enabled {boolean} — pass `true` to show the watermark, `false` to hide it. Defaults to `false` if not called.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    pageHeader(header):
    - Description: "Sets the HTML string injected as the page header on every printed page."
    - @param header {string} — raw HTML string; use empty string to suppress the header. Defaults to empty string if not called.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    pageFooter(footer):
    - Description: "Sets the HTML string injected as the page footer on every printed page."
    - @param footer {string} — raw HTML string; use empty string to suppress the footer. Defaults to empty string if not called.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    marcaDaguaInstituicao(marcaDaguaUrl):
    - Description: "Sets the institution watermark image URL applied to every page via CSS."
    - @param marcaDaguaUrl {string} — absolute URL to the watermark image. Pass null to remove. Optional — omit to suppress the watermark.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    marcaDaguaRascunho(marcaDaguaUrl: string):
    - Description: "Sets the draft watermark image URL; use with habilitarMarcaDaguaRascunho(true) to activate."
    - @param marcaDaguaUrl {string} — absolute URL to the draft watermark image.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional; no effect unless habilitarMarcaDaguaRascunho(true) is also called

    fonteTamanho(tamanho):
    - Description: "Sets the base font size for question text in pixels."
    - @param tamanho {number} — font size in pixels; must be a numeric value. Throws if NaN. Defaults to 12 if not called.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    gabarito():
    - Description: "Enables answer key (gabarito) rendering mode — answers are shown alongside questions."
    - No parameters.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional; omit for student-facing output

    rascunho(quantidadeFolhasRascunho):
    - Description: "Sets the number of blank draft (rascunho) pages to append at the end of the document."
    - @param quantidadeFolhasRascunho {number} — integer count of blank pages; must be numeric. Throws if NaN. Defaults to 0 if not called.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    rascunhoHtml(rascunhoHtml):
    - Description: "Sets the HTML template used for draft pages; overrides the default blank page template."
    - @param rascunhoHtml {string} — HTML string for the draft page body. Null/undefined is coerced to empty string.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    folhaDeRosto({ header, content, footer }):
    - Description: "Sets the cover sheet (folha de rosto) HTML content shown before the first question page."
    - @param header {string} — HTML string for the cover sheet header; required in the argument object (throws if null).
    - @param content {string} — HTML string for the cover sheet body; required in the argument object (throws if null).
    - @param footer {string} — HTML string for the cover sheet footer; required in the argument object (throws if null).
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional; omit for assessments without a cover sheet

    colunas(quantidade):
    - Description: "Sets the number of columns for question layout on each page."
    - @param quantidade {number} — must be 1 or 2. Throws if not numeric or outside this range.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional; defaults to 1 column

    identificacao(identificacao):
    - Description: "Sets the assessment identification string shown in the printed header area."
    - @param identificacao {string} — identification label text; defaults to empty string.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    paginacao():
    - Description: "Activates page number rendering in the document footer."
    - No parameters.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional; omit to suppress page numbers

    ordemAlternativa(tipoOrdenacao):
    - Description: "Sets the ordering mode applied to multiple-choice alternatives."
    - @param tipoOrdenacao {number} — one of: 0 (NAO_EMBARALHAR, no shuffle), 1 (ALEATORIO, random), 2 (ASCENDENTE, ascending), 3 (DESCENDENTE, descending). Throws for invalid values.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional; defaults to 0 (no shuffle)

    tipoAlternativa(tipoAlternativa):
    - Description: "Sets the alternative label style (e.g., letter case or numbering scheme) used in multiple-choice questions."
    - @param tipoAlternativa {any} — value passed through to the renderer; acceptable values depend on the template configuration.
    - @returns {LayoutAvaliacaoBuilder}
    - Note: optional

    build(input: AssessmentInput):
    - Description: "Builds and freezes the final layout output from the given assessment input. Call this last after all configuration methods."
    - @param input {AssessmentInput} — typed assessment data including questions, attachments, and optional layout overrides. Use fromProvaModelo() to convert a raw backend provaModelo3 object to this type.
    - @returns {Readonly<{ layoutHtml: string; cssVars: Record<string, string>; folhaDeRosto: object; header: string; footer: string; comMarcaDaguaRascunho: boolean; ordemAlternativa: number; tipoAlternativa: any; handlers: never[] }>} — frozen result object. layoutHtml is the full HTML to inject into the DOM. cssVars are CSS custom properties to apply to the container. handlers is always an empty array; Paged.js handlers are registered externally by the host application.
    - Note: REQUIRED — must be called to produce output

    After adding all JSDoc blocks, verify TypeScript still compiles with no new errors by running `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>grep -c "@param" src/LayoutAvaliacaoBuilder.ts</automated>
    <automated>grep -c "@returns" src/LayoutAvaliacaoBuilder.ts</automated>
    <automated>grep -c "habilitarMarcaDaguaRascunho" src/LayoutAvaliacaoBuilder.ts</automated>
    <automated>grep -c "marcaDaguaRascunho" src/LayoutAvaliacaoBuilder.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 16 public methods (habilitarMarcaDaguaRascunho, pageHeader, pageFooter, marcaDaguaInstituicao, marcaDaguaRascunho, fonteTamanho, gabarito, rascunho, rascunhoHtml, folhaDeRosto, colunas, identificacao, paginacao, ordemAlternativa, tipoAlternativa, build) has a JSDoc block comment (/** */) directly above its definition
    - `grep -c "@param" src/LayoutAvaliacaoBuilder.ts` returns >= 14 (most methods have at least one @param; gabarito and paginacao have none)
    - `grep -c "@returns" src/LayoutAvaliacaoBuilder.ts` returns exactly 16 (one per method)
    - `npx tsc --noEmit` exits with code 0 — no type errors introduced
    - No method signature or method body was changed — only JSDoc comments were added above method definitions
    - The JSDoc on build() references AssessmentInput by name and describes the return value including layoutHtml and handlers
  </acceptance_criteria>
  <done>
    - All 16 public methods have JSDoc comments with @param (where applicable), @returns, and optional/required notes
    - TypeScript compilation succeeds with no new errors
    - An IDE hovering over any builder method call shows the inline documentation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file modification | src/LayoutAvaliacaoBuilder.ts is modified — risk is introducing syntax errors or changing method behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | src/LayoutAvaliacaoBuilder.ts | mitigate | Read the full file before editing; add comments only above method definitions; verify tsc --noEmit exits 0 after changes |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed in this plan |
</threat_model>

<verification>
- All 16 public methods have JSDoc: `grep -c "/\*\*" src/LayoutAvaliacaoBuilder.ts` returns >= 16
- @param tags present: `grep -c "@param" src/LayoutAvaliacaoBuilder.ts` returns >= 14
- @returns tags present: `grep -c "@returns" src/LayoutAvaliacaoBuilder.ts` returns >= 16
- TypeScript clean: `npx tsc --noEmit` exits 0
- build() JSDoc references AssessmentInput: `grep "AssessmentInput" src/LayoutAvaliacaoBuilder.ts` returns >= 2 (import + JSDoc)
</verification>

<success_criteria>
- An IDE user hovering over any LayoutAvaliacaoBuilder method sees a description, valid parameter values, and whether the method is optional
- build() JSDoc states what the return value contains and that AssessmentInput is the parameter type
- No behavioral changes — TypeScript compilation succeeds, no method signatures altered
</success_criteria>

<output>
Create `.planning/phases/03-documentation/03-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| JSDoc on habilitarMarcaDaguaRascunho | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on pageHeader | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on pageFooter | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on marcaDaguaInstituicao | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on marcaDaguaRascunho | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on fonteTamanho | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on gabarito | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on rascunho | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on rascunhoHtml | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on folhaDeRosto | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on colunas | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on identificacao | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on paginacao | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on ordemAlternativa | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on tipoAlternativa | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
| JSDoc on build | JSDoc comment | src/LayoutAvaliacaoBuilder.ts |
