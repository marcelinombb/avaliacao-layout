---
phase: 03-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "A developer can read README.md and construct a minimal AssessmentInput, call createLayout(), and invoke build() — all required fields shown"
    - "A developer who already uses the provaModelo3 backend shape can find the fromProvaModelo() migration section and see a complete before/after example"
    - "README documents every required field of AssessmentInput and QuestionInput (order, value, type are required; all others optional)"
    - "README documents what the build() return value contains (layoutHtml, cssVars, folhaDeRosto, header, footer, comMarcaDaguaRascunho, ordemAlternativa, tipoAlternativa, handlers)"
  artifacts:
    - path: "README.md"
      provides: "Library onboarding documentation"
      contains: "AssessmentInput"
    - path: "README.md"
      provides: "Migration guide"
      contains: "fromProvaModelo"
  key_links:
    - from: "README.md minimal example"
      to: "src/types/AssessmentInput.ts"
      via: "AssessmentInput interface field names match README example"
      pattern: "order.*value.*type"
    - from: "README.md migration section"
      to: "src/adapter/ProvaModelo3Adapter.ts"
      via: "fromProvaModelo function name matches README"
      pattern: "fromProvaModelo"
---

<objective>
Create README.md for the avaliacao-layout library covering the new AssessmentInput contract (DOCS-01) and the fromProvaModelo() migration path for existing consumers (DOCS-02).

Purpose: A developer new to the library can understand the full input contract and use the library correctly from the README alone. An existing consumer who previously passed the raw provaModelo3 backend shape can find the adapter migration path.

Output: README.md at the repository root.
</objective>

## Phase Goal

**As a** developer integrating the avaliacao-layout library, **I want to** understand the input contract and builder API from the README alone, **so that** I can integrate the library without reading source code.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/AssessmentInput.ts
@src/adapter/ProvaModelo3Adapter.ts
@src/index.ts
@src/LayoutAvaliacaoBuilder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with AssessmentInput contract and fromProvaModelo migration (DOCS-01, DOCS-02)</name>
  <files>README.md</files>
  <read_first>
    - src/types/AssessmentInput.ts — source of truth for all interface names and fields; every field shown in README examples must match exactly
    - src/adapter/ProvaModelo3Adapter.ts — source of truth for fromProvaModelo function signature and what it accepts
    - src/index.ts — source of truth for what is exported from the package (AssessmentInput, QuestionInput, AttachmentInput, AssessmentLayoutInput, ReferenceInput, fromProvaModelo, createLayout, LayoutAvaliacaoBuilder, LayoutRenderer, latexParser, replacePlaceholders)
    - src/LayoutAvaliacaoBuilder.ts — source of truth for build() return shape and all builder method signatures
  </read_first>
  <action>
    Create README.md at the repository root. The file does not exist — write it from scratch. Structure it as follows:

    Section 1 — "avaliacao-layout": one-paragraph description of what the library does (converts a typed AssessmentInput into a paginated, PDF-ready HTML document using Paged.js).

    Section 2 — "Installation": show `npm install avaliacao-layout` followed by the import line `import { createLayout, AssessmentInput } from 'avaliacao-layout'`.

    Section 3 — "Quick Start" (DOCS-01): Show a complete minimal example that a developer can run immediately. The example must:
    - Import `createLayout` and `AssessmentInput` from 'avaliacao-layout'
    - Construct an `AssessmentInput` object showing all REQUIRED fields: `questions` array where each entry has `order` (number), `value` (number), `type` (string), and `visualizaQuestaoRaw` (string containing the serialized question JSON). Include `id`, `title`, and `layout` as optional but recommended.
    - Call `createLayout()` to get a builder instance
    - Chain `.pageHeader(htmlString)` and `.pageFooter(htmlString)` showing those methods exist
    - Call `.build(input)` and destructure `{ layoutHtml, cssVars }` from the result
    - Show how to inject `layoutHtml` into the DOM and apply `cssVars` as CSS custom properties on the container element
    Do NOT use fenced code blocks with language identifiers that conflict with the action prose — the action describes what to write, not the exact text.

    Section 4 — "AssessmentInput Reference" (DOCS-01): Document the AssessmentInput interface. For each field list its TypeScript type, whether it is optional or required, and one sentence describing what it does. Fields to document:
    - `id?: number | string` — optional assessment identifier
    - `title?: string` — optional assessment title
    - `questions?: QuestionInput[]` — array of questions to render; required in practice for a useful output
    - `attachments?: AttachmentInput[]` — array of annexe/attachment blocks shown before questions
    - `layout?: AssessmentLayoutInput` — layout configuration overrides (font size, columns, watermark, etc.)

    Then document QuestionInput fields. Mark `order` (number), `value` (number), and `type` (string) as REQUIRED. For `visualizaQuestaoRaw` note it is the serialized JSON string produced by the backend containing the question body, and that the library calls JSON.parse on it internally. Document each field type and its purpose in one sentence. Fields: `id`, `order`, `value`, `type`, `visualizaQuestaoRaw`, `customOrder`, `reference`, `orderAlternative`, `title`, `linhasBranco`, `quebraPagina`, `visualizaResposta`, `tipoLinha`, `numeroLinhas`.

    Section 5 — "Builder Methods Reference": List each method on LayoutAvaliacaoBuilder as a table row: method name, parameter(s) with types, return type, description. Methods to include from src/LayoutAvaliacaoBuilder.ts: `pageHeader`, `pageFooter`, `fonteTamanho`, `colunas`, `paginacao`, `gabarito`, `rascunho`, `rascunhoHtml`, `folhaDeRosto`, `marcaDaguaInstituicao`, `marcaDaguaRascunho`, `habilitarMarcaDaguaRascunho`, `ordemAlternativa`, `tipoAlternativa`, `identificacao`, `build`. For each method show the actual parameter name and type from the source file.

    Section 6 — "build() Return Value": Document what Object.freeze({...}) returns. Show each key: `layoutHtml` (string, the full HTML document to inject into the DOM), `cssVars` (object with keys `--layout-font-size`, `--layout-watermark-rascunho`, `--layout-watermark-instituicao`, `--layout-identificacao`), `folhaDeRosto` (object with header/content/footer), `header` (string), `footer` (string), `comMarcaDaguaRascunho` (boolean), `ordemAlternativa` (number), `tipoAlternativa` (any), `handlers` (always empty array — injected externally by the host application).

    Section 7 — "Migrating from provaModelo3" (DOCS-02): Explain that before Phase 2, consumers passed the raw backend provaModelo3 shape directly. Show the before/after pattern:
    - Before: `builder.build(provaModelo3)` — where provaModelo3 is the raw backend response object with `prova` and `listaProvaQuestao` top-level keys
    - After: `import { fromProvaModelo } from 'avaliacao-layout'` then `builder.build(fromProvaModelo(provaModelo3))`
    Explain that `fromProvaModelo` maps `prova.id`, `prova.descricao`, `listaProvaQuestao`, `prova.listaProvaAnexo`, and `prova.layout` (merged with `prova.quebraQuestao`) into the AssessmentInput shape. Note that no backend changes are required — the adapter accepts the current provaModelo3 shape exactly.

    Section 8 — "Paged.js Rendering": Brief note that after calling build() the consumer must initialize Paged.js (via LayoutRenderer / PagedJsRenderer) passing the layoutHtml and cssVars. This is the browser-side pagination step.

    Do not include a CHANGELOG, LICENSE, or Contributing section — those are out of scope for this plan.
  </action>
  <verify>
    <automated>grep -c "fromProvaModelo" README.md</automated>
    <automated>grep -c "AssessmentInput" README.md</automated>
    <automated>grep -c "visualizaQuestaoRaw" README.md</automated>
    <automated>grep -c "build(" README.md</automated>
    <automated>grep -c "layoutHtml" README.md</automated>
    <automated>grep -c "cssVars" README.md</automated>
    <automated>grep -c "quebraQuestao" README.md</automated>
  </verify>
  <done>
    - README.md exists at the repository root
    - Section "Quick Start" contains a complete minimal example constructing an AssessmentInput and calling createLayout().build()
    - Section "AssessmentInput Reference" documents all five top-level fields of AssessmentInput and all QuestionInput fields; marks order, value, type as required
    - Section "Builder Methods Reference" lists all 16 builder methods with parameters and descriptions
    - Section "build() Return Value" documents all 9 keys returned by build()
    - Section "Migrating from provaModelo3" shows the before/after fromProvaModelo pattern and states no backend changes are needed
    - grep -c "fromProvaModelo" README.md returns 3 or more (import, usage example, explanation)
    - grep -c "AssessmentInput" README.md returns 5 or more
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content | Static documentation file — no code execution, no trust boundary crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | README.md | accept | Documentation is intentionally public; no secrets or credentials are referenced |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed in this plan |
</threat_model>

<verification>
- README.md exists at repository root: `test -f README.md`
- fromProvaModelo appears in README: `grep -c "fromProvaModelo" README.md` returns >= 3
- AssessmentInput appears in README: `grep -c "AssessmentInput" README.md` returns >= 5
- All required QuestionInput fields documented: `grep "order" README.md` and `grep "value" README.md` and `grep "type" README.md` each return a match
- build() return value section present: `grep "layoutHtml" README.md` returns a match
- Migration section present: `grep "Migrating" README.md` returns a match
</verification>

<success_criteria>
- A developer new to the library can read README.md and construct a working AssessmentInput without reading source code
- A developer using the raw provaModelo3 backend shape can find the fromProvaModelo migration path and apply it in under 5 minutes
- README references only exported symbols that actually exist in src/index.ts
</success_criteria>

<output>
Create `.planning/phases/03-documentation/03-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| README.md | New file | README.md |
| "Quick Start" section | Documentation section | README.md |
| "AssessmentInput Reference" section | Documentation section | README.md |
| "Builder Methods Reference" section | Documentation section | README.md |
| "build() Return Value" section | Documentation section | README.md |
| "Migrating from provaModelo3" section | Documentation section | README.md |
| "Paged.js Rendering" section | Documentation section | README.md |
</output>
