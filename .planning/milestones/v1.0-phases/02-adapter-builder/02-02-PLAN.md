---
phase: "02-adapter-builder"
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/LayoutAvaliacaoBuilder.ts
autonomous: true
requirements:
  - BUILDER-01
  - BUILDER-02
  - INPUT-03

must_haves:
  truths:
    - "builder.build(fromProvaModelo3(raw)) compiles and executes without error"
    - "LayoutAvaliacaoBuilder exposes habilitarMarcaDaguaRascunho(boolean) and marcaDaguaRascunho(string) — no marcaDaquaRascunho typo variant"
    - "pagina field does not appear in LayoutAvaliacaoBuilder class body or constructor"
    - "build() parameter is typed as AssessmentInput and reads quebraQuestao from input.layout?.quebraQuestao"
  artifacts:
    - path: "src/LayoutAvaliacaoBuilder.ts"
      provides: "Updated builder with clean API and AssessmentInput-typed build()"
      contains: "build(input: AssessmentInput)"
  key_links:
    - from: "src/LayoutAvaliacaoBuilder.ts build()"
      to: "src/LayoutAvaliacao.ts constructor"
      via: "new LayoutAvaliacao(input, {...})"
      pattern: "new LayoutAvaliacao\\(input,"
    - from: "src/LayoutAvaliacaoBuilder.ts"
      to: "src/types/AssessmentInput.ts"
      via: "named import"
      pattern: "import.*AssessmentInput.*from.*types/AssessmentInput"
---

## Phase Goal

**As a** developer consuming the library, **I want to** pass a raw `provaModelo3` backend response through `fromProvaModelo3()`, **so that** I receive a typed `AssessmentInput` that the builder accepts without any backend changes.

<objective>
Update LayoutAvaliacaoBuilder to complete the end-to-end integration: wire build() to accept AssessmentInput, fix the marcaDagua method duplication, and remove the dead pagina field. After this plan, the full usage pattern `builder.build(fromProvaModelo3(raw))` works correctly.

Purpose: Close the remaining gap in the adapter integration (builder side) and resolve two known API defects (BUILDER-01, BUILDER-02).

Output: src/LayoutAvaliacaoBuilder.ts updated with clean API and AssessmentInput-typed build().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-adapter-builder/02-CONTEXT.md
@.planning/phases/02-adapter-builder/02-PATTERNS.md
@src/types/AssessmentInput.ts
@.planning/phases/02-adapter-builder/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up LayoutAvaliacaoBuilder — fix marcaDagua methods, remove pagina, update build() signature</name>
  <files>src/LayoutAvaliacaoBuilder.ts</files>
  <read_first>
    - src/LayoutAvaliacaoBuilder.ts — read in full; all four changes are targeted at specific line ranges: marcaDaguaRascunho (lines 54–57), marcaDaquaRascunho (lines 74–77), pagina field (line 15 and lines 38–41), build() signature (lines 162–172)
    - src/types/AssessmentInput.ts — confirm AssessmentLayoutInput has quebraQuestao?: boolean (line 34); this is the field that replaces provaModelo.prova.quebraQuestao in build()
    - .planning/phases/02-adapter-builder/02-PATTERNS.md — exact before/after for all four changes to LayoutAvaliacaoBuilder.ts (lines 77–168)
  </read_first>
  <action>
    Make four targeted changes to src/LayoutAvaliacaoBuilder.ts per decisions D-05, D-08, D-09, D-10, D-11:

    1. Add AssessmentInput import at the top of the file (after the existing LayoutAvaliacao import on line 1):
       `import { AssessmentInput } from './types/AssessmentInput';`

    2. Remove the `pagina` class property declaration (line 15: `pagina: any;`) and remove its constructor initialization block (lines 38–41: the `this.pagina = { header: "", footer: "" };` block). Do not change any other constructor property. (per D-11)

    3. Replace the boolean marcaDaguaRascunho method (lines 54–57) with habilitarMarcaDaguaRascunho:
       Old: method named `marcaDaguaRascunho(comMarcaDagua)` that assigns `this.comMarcaDaguaRascunho = comMarcaDagua`
       New method name: `habilitarMarcaDaguaRascunho`, parameter: `enabled: boolean`, body: `this.comMarcaDaguaRascunho = enabled; return this;`
       (per D-10 — hard rename, no deprecated alias)

    4. Replace the typo-named marcaDaquaRascunho method (lines 74–77) with correctly-spelled marcaDaguaRascunho:
       Old: method named `marcaDaquaRascunho(marcaDaguaUrl)` that assigns `this._marcaDaquaRascunho = marcaDaguaUrl`
       New method name: `marcaDaguaRascunho`, parameter: `marcaDaguaUrl: string`, body: `this._marcaDaquaRascunho = marcaDaguaUrl; return this;`
       (per D-09 — hard rename, no deprecated alias)

    5. Update build() signature and internal field reads:
       - Change `build(provaModelo)` to `build(input: AssessmentInput)` (per D-05)
       - Change `new LayoutAvaliacao(provaModelo, {` to `new LayoutAvaliacao(input, {` (per D-07)
       - Change `quebraQuestao: provaModelo.prova.quebraQuestao` to `quebraQuestao: input.layout?.quebraQuestao` (per specifics note in 02-CONTEXT.md)
       - All other properties in the layoutOptions object remain unchanged

    The `_marcaDaquaRascunho` private field name (line 17) stays as-is — it is an internal implementation detail, not a public API name.
  </action>
  <verify>
    <automated>cd /home/marcelino/Documentos/avaliacao-layout && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep for `pagina` in src/LayoutAvaliacaoBuilder.ts returns 0 matches (field removed from property list and constructor)
    - grep for `marcaDaquaRascunho` in src/LayoutAvaliacaoBuilder.ts returns 0 matches (typo variant completely removed)
    - grep for `habilitarMarcaDaguaRascunho` in src/LayoutAvaliacaoBuilder.ts returns exactly 1 match (the new method)
    - grep for `marcaDaguaRascunho` in src/LayoutAvaliacaoBuilder.ts returns exactly 1 match (the URL-setter method, not the old boolean one)
    - grep for `build(input: AssessmentInput)` in src/LayoutAvaliacaoBuilder.ts returns exactly 1 match
    - grep for `provaModelo.prova.quebraQuestao` in src/LayoutAvaliacaoBuilder.ts returns 0 matches
    - grep for `input.layout?.quebraQuestao` in src/LayoutAvaliacaoBuilder.ts returns 1 match
    - grep for `new LayoutAvaliacao(input,` in src/LayoutAvaliacaoBuilder.ts returns 1 match
    - grep for `AssessmentInput` import in src/LayoutAvaliacaoBuilder.ts returns 1 match
    - tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>LayoutAvaliacaoBuilder.ts updated — build() accepts AssessmentInput, marcaDagua duplication resolved, pagina field removed, tsc passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full integration: src/adapter/ProvaModelo3Adapter.ts created, LayoutAvaliacao._mapToEntity() updated to read QuestionInput fields, LayoutAvaliacaoBuilder.build() updated to accept AssessmentInput. The complete path — fromProvaModelo3(raw) → build() → rendered HTML — is now wired.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the browser dev harness (port 10001)
    2. Open http://localhost:10001 in the browser
    3. Open DevTools console — verify no errors on page load
    4. Confirm the assessment renders visually (questions appear, pagination works, no blank page)
    5. Compare with the previous rendering (pixel-identical output expected per Phase 2 success criterion 2)
    6. Optionally: in DevTools console, run `fromProvaModelo3(provaModelo3)` (if the fixture is accessible as a global) and inspect that the returned object has questions[], attachments[], layout, id, and title fields
  </how-to-verify>
  <resume-signal>Type "approved" if rendering is pixel-identical to the previous output, or describe any visual differences</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build() | AssessmentInput from adapter crosses into builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | build() input | accept | Same trust boundary as plan 01 — single-consumer internal library; AssessmentInput is produced by fromProvaModelo3() from the same team's backend |
| T-02-04 | Elevation of Privilege | Removed pagina field | accept | Dead field removal reduces attack surface, not increases it |
| T-02-SC | Tampering | npm installs | accept | No new packages installed in this plan — pure source-file changes only |
</threat_model>

<verification>
After task 1 and the human-verify checkpoint:
1. `npx tsc --noEmit` exits 0
2. No `marcaDaquaRascunho` in builder source
3. No `pagina` in builder source
4. `build(input: AssessmentInput)` in builder source
5. Browser harness renders pixel-identically to Phase 1 output
</verification>

<success_criteria>
- BUILDER-01: `marcaDaguaRascunho(url: string)` and `habilitarMarcaDaguaRascunho(boolean)` present; no `marcaDaquaRascunho` typo variant
- BUILDER-02: `pagina` field absent from LayoutAvaliacaoBuilder
- INPUT-03: `builder.build(fromProvaModelo3(raw))` compiles and renders identical HTML output
- tsc --noEmit passes with no errors
- Browser dev harness confirms pixel-identical rendering
</success_criteria>

<output>
Create `.planning/phases/02-adapter-builder/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `LayoutAvaliacaoBuilder.build(input: AssessmentInput)` | method signature change | `src/LayoutAvaliacaoBuilder.ts` |
| `habilitarMarcaDaguaRascunho` | new method (renamed from boolean marcaDaguaRascunho) | `src/LayoutAvaliacaoBuilder.ts` |
| `marcaDaguaRascunho` | updated method (correct spelling, replaces marcaDaquaRascunho) | `src/LayoutAvaliacaoBuilder.ts` |
| `marcaDaquaRascunho` | REMOVED — typo variant deleted | `src/LayoutAvaliacaoBuilder.ts` |
| `pagina` property | REMOVED — dead field deleted | `src/LayoutAvaliacaoBuilder.ts` |
