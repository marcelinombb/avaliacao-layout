---
phase: 02-adapter-builder
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapter/ProvaModelo3Adapter.ts
  - index.html
autonomous: false
gap_closure: true
requirements:
  - INPUT-03

must_haves:
  truths:
    - "fromProvaModelo3(provaModelo3) returns attachments populated from prova.listaProvaAnexo, not from root"
    - "fromProvaModelo3(provaModelo3) returns layout.quebraQuestao mapped from prova.quebraQuestao"
    - "index.html passes the adapter result to build(), not the raw provaModelo3 object"
    - "index.html calls marcaDaguaRascunho (correct spelling), not the deleted marcaDaquaRascunho"
    - "Browser dev harness renders the assessment with no console errors"
  artifacts:
    - path: "src/adapter/ProvaModelo3Adapter.ts"
      provides: "Corrected data-path destructuring for listaProvaAnexo and quebraQuestao"
      contains: "prova?.listaProvaAnexo"
    - path: "index.html"
      provides: "Harness wired through fromProvaModelo3 adapter"
      contains: "fromProvaModelo3(provaModelo3)"
  key_links:
    - from: "index.html"
      to: "AvaliacaoLayout.fromProvaModelo3"
      via: "const assessmentInput = AvaliacaoLayout.fromProvaModelo3(provaModelo3)"
      pattern: "fromProvaModelo3\\(provaModelo3\\)"
    - from: "src/adapter/ProvaModelo3Adapter.ts"
      to: "prova.listaProvaAnexo"
      via: "const listaProvaAnexo = prova?.listaProvaAnexo"
      pattern: "prova\\?\\.listaProvaAnexo"
    - from: "src/adapter/ProvaModelo3Adapter.ts"
      to: "prova.quebraQuestao"
      via: "quebraQuestao: prova?.quebraQuestao"
      pattern: "quebraQuestao.*prova\\?\\.quebraQuestao"
---

<objective>
Fix two targeted data-path defects in ProvaModelo3Adapter.ts and update the index.html dev harness to
wire through the adapter. These are the only items blocking INPUT-03.

Purpose: The fixture places listaProvaAnexo at prova.listaProvaAnexo (not at the provaModelo3 root),
and quebraQuestao at prova.quebraQuestao (not inside prova.layout). Both are currently silently
disconnected. The harness bypasses the adapter entirely and calls a deleted method.

Output: ProvaModelo3Adapter.ts with correct source paths; index.html wired to fromProvaModelo3 with
the correct marcaDaguaRascunho spelling.
</objective>

<execution_context>
@/home/marcelino/Documentos/avaliacao-layout/.planning/phases/02-adapter-builder/02-03-PLAN.md
</execution_context>

<context>
@/home/marcelino/Documentos/avaliacao-layout/.planning/ROADMAP.md
@/home/marcelino/Documentos/avaliacao-layout/.planning/phases/02-adapter-builder/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ProvaModelo3Adapter.ts data-path defects</name>
  <files>src/adapter/ProvaModelo3Adapter.ts</files>
  <read_first>
    - src/adapter/ProvaModelo3Adapter.ts (full file — 34 lines)
    - public/prova-modelo.js (fixture — confirm prova.listaProvaAnexo at line 39, prova.quebraQuestao at line 75)
  </read_first>
  <action>
Two targeted changes to src/adapter/ProvaModelo3Adapter.ts:

Change 1 — line 4, destructure fix:
  Before: const { prova, listaProvaQuestao, listaProvaAnexo } = provaModelo3;
  After:  const { prova, listaProvaQuestao } = provaModelo3;
  Then add on a new line 5: const listaProvaAnexo = prova?.listaProvaAnexo;

  Rationale: The fixture has listaProvaAnexo at provaModelo3.prova.listaProvaAnexo (fixture line 39),
  not at the provaModelo3 root. The root destructure always yields undefined, silently producing an
  empty attachments array.

Change 2 — line 25 (will shift to ~26 after Change 1), layout mapping:
  Before: const layout: AssessmentLayoutInput = prova?.layout || {};
  After:  const layout: AssessmentLayoutInput = { ...(prova?.layout || {}), quebraQuestao: prova?.quebraQuestao };

  Rationale: The fixture has quebraQuestao at provaModelo3.prova.quebraQuestao (fixture line 75),
  not inside prova.layout. The original mapping never includes it in the layout object, so
  input.layout?.quebraQuestao is always undefined and the question-break feature is silently disabled.

No other lines change. The return statement, questions mapping, and all imports remain untouched.
  </action>
  <verify>
    <automated>grep -n "prova?.listaProvaAnexo" /home/marcelino/Documentos/avaliacao-layout/src/adapter/ProvaModelo3Adapter.ts</automated>
    <automated>grep -n "quebraQuestao" /home/marcelino/Documentos/avaliacao-layout/src/adapter/ProvaModelo3Adapter.ts</automated>
    <automated>grep -c "listaProvaAnexo } = provaModelo3" /home/marcelino/Documentos/avaliacao-layout/src/adapter/ProvaModelo3Adapter.ts || true</automated>
    <automated>cd /home/marcelino/Documentos/avaliacao-layout && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep for "prova?.listaProvaAnexo" returns at least one match in ProvaModelo3Adapter.ts
    - grep for "quebraQuestao" returns a line containing "prova?.quebraQuestao" in ProvaModelo3Adapter.ts
    - grep for "listaProvaAnexo } = provaModelo3" returns 0 matches (old destructure removed)
    - npx tsc --noEmit exits 0 with no output
  </acceptance_criteria>
  <done>ProvaModelo3Adapter.ts reads listaProvaAnexo from prova?.listaProvaAnexo and spreads quebraQuestao: prova?.quebraQuestao into the layout object; tsc compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html harness to wire fromProvaModelo3 and fix method spelling</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 248-265 (the build() call and the watermark block)
  </read_first>
  <action>
Two targeted changes to index.html:

Change 1 — line 251, fix deleted method call:
  Before: layoutBuilder.marcaDaquaRascunho("public/assets/marcadagua/marcadagua_semfundo.png");
  After:  layoutBuilder.marcaDaguaRascunho("public/assets/marcadagua/marcadagua_semfundo.png");

  Rationale: marcaDaquaRascunho (with 'a') was removed from LayoutAvaliacaoBuilder as part of the
  builder cleanup in Phase 2. The correct public method is marcaDaguaRascunho (with 'u'). Calling
  the deleted method causes a TypeError crash when the watermark checkbox is checked.

Change 2 — line 262, wire the adapter:
  Before: let layoutResult = layoutBuilder.build(provaModelo3);
  After (replace the single line with two lines):
    const assessmentInput = AvaliacaoLayout.fromProvaModelo3(provaModelo3);
    let layoutResult = layoutBuilder.build(assessmentInput);

  Rationale: The harness was never updated to use the adapter. Passing the raw provaModelo3 object
  to build() bypasses fromProvaModelo3() entirely, so the end-to-end path is never exercised and
  the data-path defects would not have been caught through the harness.

No other lines change. The rest of the build chain (latexParser, LayoutRenderer.render) remains
untouched.
  </action>
  <verify>
    <automated>grep -n "fromProvaModelo3(provaModelo3)" /home/marcelino/Documentos/avaliacao-layout/index.html</automated>
    <automated>grep -n "marcaDaguaRascunho" /home/marcelino/Documentos/avaliacao-layout/index.html</automated>
    <automated>grep -c "build(provaModelo3)" /home/marcelino/Documentos/avaliacao-layout/index.html || true</automated>
    <automated>grep -c "marcaDaquaRascunho" /home/marcelino/Documentos/avaliacao-layout/index.html || true</automated>
  </verify>
  <acceptance_criteria>
    - grep for "fromProvaModelo3(provaModelo3)" returns at least one match in index.html
    - grep for "marcaDaguaRascunho" returns at least one match in index.html (correct spelling)
    - grep for "build(provaModelo3)" returns 0 matches (raw object no longer passed directly)
    - grep for "marcaDaquaRascunho" returns 0 matches (deleted method no longer called)
  </acceptance_criteria>
  <done>index.html calls fromProvaModelo3(provaModelo3) and passes the result to build(); the watermark method call uses the correct marcaDaguaRascunho spelling; no deleted method references remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser dev harness verification</name>
  <what-built>
    Two files were updated:
    - src/adapter/ProvaModelo3Adapter.ts: listaProvaAnexo now reads from prova?.listaProvaAnexo;
      quebraQuestao is now spread into the layout object from prova?.quebraQuestao.
    - index.html: harness now calls fromProvaModelo3(provaModelo3) and passes the result to build();
      watermark call uses correct marcaDaguaRascunho spelling.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open http://localhost:10001 in a browser.
    3. Open DevTools (F12) and go to the Console tab.
    4. Verify: no TypeError, no JSON parse errors, no warnings about undefined methods.
    5. Verify: the assessment renders with all questions visible and pagination working.
    6. Compare visually against the Phase 1 rendering — output must be pixel-identical.
    7. If the fixture's prova.listaProvaAnexo array has entries, verify annexe/reference blocks
       appear alongside their associated questions.
    8. Check the "marca d'água rascunho" checkbox in the side menu — verify no crash (previously
       a TypeError from the deleted method).
  </how-to-verify>
  <resume-signal>Type "approved" if rendering is pixel-identical and console is clean, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data → adapter | provaModelo3 object from public/prova-modelo.js is developer-controlled fixture data; no untrusted user input in this context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-gc-01 | Tampering | ProvaModelo3Adapter.ts field mapping | accept | Gap-closure edits are targeted line changes with tsc verification; no logic branches added |
| T-02-gc-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
All automated checks must pass before the human-verify checkpoint:

1. grep -n "prova?.listaProvaAnexo" src/adapter/ProvaModelo3Adapter.ts — must return a match
2. grep -n "quebraQuestao.*prova?.quebraQuestao" src/adapter/ProvaModelo3Adapter.ts — must return a match
3. grep -c "listaProvaAnexo } = provaModelo3" src/adapter/ProvaModelo3Adapter.ts — must return 0
4. npx tsc --noEmit — must exit 0 with no errors
5. grep -n "fromProvaModelo3(provaModelo3)" index.html — must return a match
6. grep -c "build(provaModelo3)" index.html — must return 0
7. grep -c "marcaDaquaRascunho" index.html — must return 0
8. grep -n "marcaDaguaRascunho" index.html — must return a match
</verification>

<success_criteria>
- ProvaModelo3Adapter.ts: listaProvaAnexo sourced from prova?.listaProvaAnexo
- ProvaModelo3Adapter.ts: layout object includes quebraQuestao: prova?.quebraQuestao
- index.html: fromProvaModelo3(provaModelo3) called before build()
- index.html: marcaDaguaRascunho used (correct spelling), marcaDaquaRascunho absent
- tsc --noEmit exits 0
- Browser: assessment renders pixel-identically, no console errors, watermark checkbox does not crash
- INPUT-03 unblocked: fromProvaModelo3() adapter is exercised end-to-end in the dev harness
</success_criteria>

## Artifacts this phase produces

| File | Change Type | Description |
|------|-------------|-------------|
| src/adapter/ProvaModelo3Adapter.ts | Modified | Line 4: remove listaProvaAnexo from root destructure; add line reading from prova?.listaProvaAnexo. Line ~25: spread quebraQuestao: prova?.quebraQuestao into layout object. |
| index.html | Modified | Line 251: rename marcaDaquaRascunho to marcaDaguaRascunho. Line 262: replace build(provaModelo3) with fromProvaModelo3 call + build(assessmentInput). |

<output>
Create `.planning/phases/02-adapter-builder/02-03-SUMMARY.md` when done.
</output>
