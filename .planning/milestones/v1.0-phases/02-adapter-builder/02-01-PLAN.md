---
phase: "02-adapter-builder"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapter/ProvaModelo3Adapter.ts
  - src/LayoutAvaliacao.ts
  - src/index.ts
autonomous: true
requirements:
  - INPUT-03

must_haves:
  truths:
    - "fromProvaModelo() is callable from a browser dev harness and returns an AssessmentInput-shaped object with questions[], attachments[], layout, id, and title"
    - "_mapToEntity() reads QuestionInput fields (q.id, q.order, q.visualizaQuestaoRaw) instead of raw backend fields (q.questao.codigo, q.questao.visualizaQuestao)"
    - "JSON.parse of visualizaQuestaoRaw still occurs inside _mapToEntity() — adapter does NOT call JSON.parse"
    - "fromProvaModelo appears as a named export in src/index.ts"
  artifacts:
    - path: "src/adapter/ProvaModelo3Adapter.ts"
      provides: "fromProvaModelo adapter function"
      exports: ["fromProvaModelo"]
    - path: "src/LayoutAvaliacao.ts"
      provides: "Updated orchestrator accepting AssessmentInput"
      contains: "_mapToEntity(input: AssessmentInput)"
    - path: "src/index.ts"
      provides: "Public package entry with fromProvaModelo re-export"
      contains: "fromProvaModelo"
  key_links:
    - from: "src/adapter/ProvaModelo3Adapter.ts"
      to: "src/types/AssessmentInput.ts"
      via: "named import"
      pattern: "import.*AssessmentInput.*from.*types/AssessmentInput"
    - from: "src/LayoutAvaliacao.ts"
      to: "src/types/AssessmentInput.ts"
      via: "named import"
      pattern: "import.*AssessmentInput.*from.*types/AssessmentInput"
    - from: "src/index.ts"
      to: "src/adapter/ProvaModelo3Adapter.ts"
      via: "re-export"
      pattern: "export.*fromProvaModelo.*from.*adapter/ProvaModelo3Adapter"
---

## Phase Goal

**As a** developer consuming the library, **I want to** pass a raw `provaModelo3` backend response through `fromProvaModelo()`, **so that** I receive a typed `AssessmentInput` that the builder accepts without any backend changes.

<objective>
Create the ProvaModelo3Adapter and update LayoutAvaliacao to accept AssessmentInput. Together these form the complete data-flow path: raw backend response → typed adapter output → entity mapping → rendered HTML.

Purpose: Enable the usage pattern `builder.build(fromProvaModelo(raw))` by building both ends: the adapter that produces AssessmentInput and the orchestrator that consumes it.

Output: src/adapter/ProvaModelo3Adapter.ts (new), src/LayoutAvaliacao.ts (updated), src/index.ts (fromProvaModelo re-export added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-adapter-builder/02-CONTEXT.md
@.planning/phases/02-adapter-builder/02-PATTERNS.md
@src/types/AssessmentInput.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/adapter/ProvaModelo3Adapter.ts and add re-export to index.ts</name>
  <files>src/adapter/ProvaModelo3Adapter.ts, src/index.ts</files>
  <read_first>
    - src/types/AssessmentInput.ts — all five interfaces that fromProvaModelo() must return; QuestionInput field names are the canonical mapping target
    - src/index.ts — current export block structure; the re-export must follow the pattern at line 10 (direct re-export form)
    - public/prova-modelo.js — the provaModelo3 fixture; trace prova, listaProvaQuestao, listaProvaAnexo structure to confirm field paths used in mapping
    - .planning/phases/02-adapter-builder/02-PATTERNS.md — the complete core transform pattern for fromProvaModelo() (lines 30–64) and the index.ts change (lines 300–330)
  </read_first>
  <action>
    Create the directory src/adapter/ and the file src/adapter/ProvaModelo3Adapter.ts.

    The file has exactly one export: `export function fromProvaModelo(provaModelo3): AssessmentInput`.

    Imports: named import of AssessmentInput, QuestionInput, AttachmentInput, AssessmentLayoutInput from '../types/AssessmentInput' — no other imports. No default export. No try/catch.

    Destructure provaModelo3 as `const { prova, listaProvaQuestao, listaProvaAnexo } = provaModelo3`.

    Map listaProvaQuestao to questions: QuestionInput[] using the field mapping table from 02-PATTERNS.md (lines 41–51). Key rule: visualizaQuestaoRaw is q.questao.visualizaQuestao passed through as-is — no JSON.parse. Use optional chaining (?./) for prova fields; use `|| []` for listaProvaQuestao and listaProvaAnexo defaults.

    Map listaProvaAnexo to attachments: AttachmentInput[] directly (same shape). Map prova?.layout to layout: AssessmentLayoutInput. Return { id: prova?.id, title: prova?.descricao, questions, attachments, layout }.

    In src/index.ts, add a direct re-export line after the existing export block:
    `export { fromProvaModelo } from './adapter/ProvaModelo3Adapter';`
    Do not modify the existing export block — append the new re-export as a separate statement (per D-04).
  </action>
  <verify>
    <automated>cd /home/marcelino/Documentos/avaliacao-layout && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/adapter/ProvaModelo3Adapter.ts exists and exports exactly one named symbol: fromProvaModelo
    - grep for `export function fromProvaModelo` returns exactly 1 match in the new file
    - grep for `fromProvaModelo` in src/index.ts returns exactly 1 match (the re-export line)
    - The re-export in index.ts uses the direct form: `export { fromProvaModelo } from './adapter/ProvaModelo3Adapter'`
    - The adapter file imports from '../types/AssessmentInput' and has no other imports
    - The adapter function body does NOT contain JSON.parse
    - tsc --noEmit exits with code 0 (no new type errors)
  </acceptance_criteria>
  <done>src/adapter/ProvaModelo3Adapter.ts created with correct mapping logic; fromProvaModelo re-exported from src/index.ts; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update LayoutAvaliacao to accept AssessmentInput</name>
  <files>src/LayoutAvaliacao.ts</files>
  <read_first>
    - src/LayoutAvaliacao.ts — read in full; changes touch class property (line 18), constructor (lines 21–24), avalicaoHtml() (line 28), and _mapToEntity() (lines 41–107)
    - src/types/AssessmentInput.ts — QuestionInput field names (lines 44–59); these are the fields _mapToEntity() must now read from q.* instead of q.questao.* and q.*
    - .planning/phases/02-adapter-builder/02-PATTERNS.md — the exact before/after for each change in LayoutAvaliacao.ts (lines 174–295)
  </read_first>
  <action>
    Make four targeted changes to src/LayoutAvaliacao.ts per D-06:

    1. Add import at top of file (after existing imports line 4):
       `import { AssessmentInput } from './types/AssessmentInput';`

    2. Change class property from `provaModelo: any` to `input: AssessmentInput`.

    3. Change constructor parameter from `provaModelo: any` to `input: AssessmentInput` and update the property assignment from `this.provaModelo = provaModelo` to `this.input = input`.

    4. In avalicaoHtml(), change `this._mapToEntity(this.provaModelo)` to `this._mapToEntity(this.input)`.

    5. Update _mapToEntity():
       - Change signature from `_mapToEntity(rawData)` to `_mapToEntity(input: AssessmentInput)`
       - Replace the destructure of rawData with: `const { questions: rawQuestions, attachments: listaProvaAnexo, layout, id, title } = input;` and `const listaProvaQuestao = rawQuestions || [];`
       - In the per-question mapping inside the .map() callback, replace all reads of the raw backend shape with QuestionInput field reads:
         - `q.questao.codigo` → `q.id`
         - `q.ordem` → `q.order`
         - `q.titulo` → `q.title`
         - `q.ordemPersonalizada` → `q.customOrder`
         - `q.valor` → `q.value`
         - `q.questao.tipoQuestao` → `q.type`
         - `q.questao.referencia` → `q.reference`
         - `q.ordemAlternativa` → `q.orderAlternative`
         - `q.questao.visualizaQuestao` → `q.visualizaQuestaoRaw` (the JSON.parse line: `JSON.parse(q.visualizaQuestaoRaw)`)
       - The post-new-Question() field assignments (q.linhasBranco, q.quebraPagina, q.visualizaResposta, q.tipoLinha, q.numeroLinhas) already match QuestionInput field names — no change needed on those lines per 02-PATTERNS.md line 283.
       - Update the `new Assessment()` call at the end to use `input` fields directly: `id: input.id`, `title: input.title`, `attachments: listaProvaAnexo || []`, `layout: input.layout || {}`. The questions variable is already built above.

    Do not change sanitizeEmptyHtml(), the seenTitles logic, or any rendering code — only the field-read paths.
  </action>
  <verify>
    <automated>cd /home/marcelino/Documentos/avaliacao-layout && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep for `provaModelo: any` in src/LayoutAvaliacao.ts returns 0 matches
    - grep for `input: AssessmentInput` in src/LayoutAvaliacao.ts returns at least 2 matches (property and constructor parameter)
    - grep for `_mapToEntity(input: AssessmentInput)` in src/LayoutAvaliacao.ts returns 1 match
    - grep for `q.questao.codigo` in src/LayoutAvaliacao.ts returns 0 matches
    - grep for `q.questao.visualizaQuestao` in src/LayoutAvaliacao.ts returns 0 matches
    - grep for `q.visualizaQuestaoRaw` in src/LayoutAvaliacao.ts returns 1 match (the JSON.parse line)
    - grep for `AssessmentInput` import in src/LayoutAvaliacao.ts returns 1 match
    - tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>LayoutAvaliacao.ts updated — constructor, property, and _mapToEntity() all accept AssessmentInput; all raw backend field reads replaced with QuestionInput field reads; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → fromProvaModelo() | Raw provaModelo3 object from backend crosses into typed adapter |
| adapter output → build() | AssessmentInput produced by adapter consumed by builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | fromProvaModelo input | accept | Adapter is a pure type-mapping function used internally by the same team; no cross-trust-boundary injection risk in current single-consumer architecture |
| T-02-02 | Information Disclosure | visualizaQuestaoRaw passthrough | accept | JSON string passes through adapter unmodified; JSON.parse errors are already caught in _mapToEntity() with console.error |
| T-02-SC | Tampering | npm installs | accept | No new packages installed in this phase — pure source-file changes only |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits 0 with no errors
2. In src/adapter/ProvaModelo3Adapter.ts: `export function fromProvaModelo` exists, no JSON.parse, no imports other than AssessmentInput types
3. In src/LayoutAvaliacao.ts: no `q.questao.*` reads remain, `_mapToEntity` accepts `AssessmentInput`
4. In src/index.ts: `fromProvaModelo` re-exported
5. Manual: `npm run dev` loads the browser harness — the existing provaModelo3 fixture renders identically to before (pixel-identical HTML output per Phase 2 success criterion 2)
</verification>

<success_criteria>
- fromProvaModelo() exported from the package and returns AssessmentInput (INPUT-03 partial — export wired)
- LayoutAvaliacao._mapToEntity() accepts AssessmentInput and reads QuestionInput fields (INPUT-03 partial — consumer wired)
- tsc --noEmit passes with no errors introduced by this plan
- No JSON.parse in the adapter file
</success_criteria>

<output>
Create `.planning/phases/02-adapter-builder/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `fromProvaModelo` | exported function | `src/adapter/ProvaModelo3Adapter.ts` |
| `src/adapter/ProvaModelo3Adapter.ts` | new file | — |
| `LayoutAvaliacao.input` | class property (replaces `provaModelo`) | `src/LayoutAvaliacao.ts` |
| `LayoutAvaliacao._mapToEntity(input: AssessmentInput)` | method signature change | `src/LayoutAvaliacao.ts` |
| `fromProvaModelo` re-export | index.ts export | `src/index.ts` |
