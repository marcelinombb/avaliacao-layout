---
phase: 01-type-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/Question.ts
  - src/domain/Assessment.ts
autonomous: true
requirements:
  - TYPES-01
  - TYPES-02

must_haves:
  truths:
    - "`QuestionContent` interface covers all eight fields rendered by Handlebars templates: `textoBase`, `comando`, `instrucao`, `fonte`, `alternativas`, `afirmacoes`, `associacoes`, `assercoes` — none typed `any`"
    - "`AfirmacaoItem`, `AssociacaoItem`, `AssociacoesContent`, `AssercoesContent`, and `TipoLinha` interfaces are declared in `src/domain/Question.ts` and used by `QuestionContent` and the `Question` class"
    - "`Question` class fields `afimacoes`, `associacoes`, `assercoes` are typed with the new concrete interfaces; `tipoLinha` is typed `TipoLinha | null` (not `string | null`)"
    - "`QuestionConstructor` interface fields `afirmacoes`, `associacoes`, `assercoes` match the concrete types used in the `Question` class"
    - "`AssessmentLayout` interface has no `[key: string]: any` index signature and no `any`-typed fields"
    - "`Attachment` interface has no `[key: string]: any` index signature (neither outer nor on `anexo`)"
    - "`npx tsc --noEmit` exits 0 after all modifications"
  artifacts:
    - path: "src/domain/Question.ts"
      provides: "Fully typed Question domain — QuestionContent with no any, five new sub-interfaces, corrected tipoLinha type"
      exports:
        - AfirmacaoItem
        - AssociacaoItem
        - AssociacoesContent
        - AssercoesContent
        - TipoLinha
        - QuestionContent
        - QuestionConstructor
        - Question
    - path: "src/domain/Assessment.ts"
      provides: "AssessmentLayout with no any and no index signature; Attachment with no index signature"
      exports:
        - Attachment
        - AssessmentLayout
        - AssessmentConstructor
        - Assessment
  key_links:
    - from: "src/domain/Question.ts"
      to: "src/rendering/templates/"
      via: "QuestionContent fields must match all {{question.visualizaQuestaoParsed.*}} accesses in .hbs files"
      pattern: "visualizaQuestaoParsed\\."
    - from: "src/domain/Question.ts"
      to: "src/rendering/components/QuadroRespostaRenderer.ts"
      via: "tipoLinha.codigo is used in a switch statement — must be number"
      pattern: "tipoLinha\\.codigo"
---

<objective>
Narrow all `any`-typed fields on the domain entities `Question` and `Assessment`, eliminating `any` from every field accessed during rendering. Add five typed sub-interfaces to `Question.ts` that cover the template-accessed fields in `QuestionContent`. Fix the `tipoLinha` type mismatch.

Purpose: Satisfies TYPES-01 (domain entities use explicit typed interfaces on rendering paths) and TYPES-02 (`QuestionContent` covers all eight Handlebars-rendered fields). These are interface-only changes — no runtime behavior changes.

Output:
- `src/domain/Question.ts` — five new exported interfaces (`AfirmacaoItem`, `AssociacaoItem`, `AssociacoesContent`, `AssercoesContent`, `TipoLinha`); updated `QuestionContent`, `QuestionConstructor`, and `Question` class fields
- `src/domain/Assessment.ts` — `AssessmentLayout` index signature removed; `any` fields narrowed; `Attachment` index signatures removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-type-foundation/01-RESEARCH.md
@.planning/phases/01-type-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed sub-interfaces and narrow any fields in src/domain/Question.ts</name>
  <files>src/domain/Question.ts</files>
  <read_first>
    - src/domain/Question.ts — read the full current file before any edits; note exact field names and types at lines 1-109
    - .planning/phases/01-type-foundation/01-PATTERNS.md — lines 127–209: the target declarations for all five new interfaces and the updated `QuestionContent`, `QuestionConstructor`, and `Question` class field list
    - .planning/phases/01-type-foundation/01-RESEARCH.md — Pitfall 4 (afirmacoes vs associacoes vs assercoes container structure differences) and Code Examples section (Complete Field Inventory: QuestionContent Fields Used in Templates)
    - src/rendering/templates/ — skim statements.hbs, associations.hbs, assertions.hbs, alternatives.hbs to confirm field access patterns match the interface declarations (these are the ground truth for QuestionContent fields)
    - src/rendering/components/QuadroRespostaRenderer.ts — confirm `tipoLinha.codigo` is used as a `number` in the switch statement (validates the `TipoLinha` interface)
    - public/prova-modelo.js — ground-truth fixture: confirms `tipoLinha` object shape `{ codigo, nome }` and `afirmacoes` item shape `{ item, descricao }`
  </read_first>
  <action>
    Modify `src/domain/Question.ts` in three stages, running `npx tsc --noEmit` after each stage.

    STAGE A — Add five sub-interfaces before the `QuestionContent` interface (insert between line 1 and the `export interface QuestionContent` declaration):

    - `AfirmacaoItem`: fields `item: string` and `descricao: string` (used in statements.hbs via `{{this.item}}` and `{{this.descricao}}`)
    - `AssociacaoItem`: same fields as `AfirmacaoItem` — `item: string`, `descricao: string` (used in associations.hbs)
    - `AssociacoesContent`: fields `coluna1: AssociacaoItem[]` and `coluna2: AssociacaoItem[]` (associations.hbs accesses `object.coluna1` and `object.coluna2`)
    - `AssercoesContent`: optional fields `assercao1?: string` and `assercao2?: string` (assertions.hbs accesses `object.assercao1` and `object.assercao2`)
    - `TipoLinha`: required fields `codigo: number` and `nome: string` (responseBox.hbs accesses `question.tipoLinha.codigo` in a switch; the fixture confirms this is always an object, not a string)

    All five interfaces use `export interface`.

    STAGE B — Update `QuestionContent` interface: replace the three `any` fields with concrete types:
    - `afirmacoes?: any[]` → `afirmacoes?: AfirmacaoItem[]`
    - `associacoes?: any` → `associacoes?: AssociacoesContent | null`
    - `assercoes?: any` → `assercoes?: AssercoesContent | null`
    - Remove `visualizaQuestaoParsed?: any` (this field is not part of the parsed content; `Question` class already has `visualizaQuestaoParsed: QuestionContent | null` as a separate class property)

    Run `npx tsc --noEmit` after Stage B. If errors appear in files outside `Question.ts`, note them but do not fix them — report in the summary.

    STAGE C — Update `QuestionConstructor` interface and `Question` class fields:

    `QuestionConstructor` interface changes:
    - `afirmacoes?: any[]` → `afirmacoes?: AfirmacaoItem[]`
    - `associacoes?: any` → `associacoes?: AssociacoesContent | null`
    - `assercoes?: any` → `assercoes?: AssercoesContent | null`
    - `content?: any` — leave as `any` (it is an internal implementation detail, not on the public rendering path per RESEARCH.md Pitfall 3)

    `Question` class field changes (class body, not constructor):
    - `afimacoes: any[]` → `afimacoes: AfirmacaoItem[]` (note: preserve the existing typo `afimacoes` — do NOT rename it; renaming a class property would require updating all call sites and is Phase 2 scope)
    - `associacoes: any` → `associacoes: AssociacoesContent | null`
    - `assercoes: any` → `assercoes: AssercoesContent | null`
    - `tipoLinha?: string | null` → `tipoLinha?: TipoLinha | null`

    Also narrow the two `any` fields on the `Reference` interface:
    - `instituicao?: any` → `instituicao?: string | null`
    - `totalRegistros?: any` → `totalRegistros?: number | null`

    Run `npx tsc --noEmit` after Stage C. The compiler must exit 0. If removing `visualizaQuestaoParsed?: any` from `QuestionContent` surfaces errors (e.g., code assigning a parsed object that includes that field), add `visualizaQuestaoParsed?: QuestionContent` back rather than `visualizaQuestaoParsed?: any`.
  </action>
  <verify>
    <automated>cd /home/marcelino/Documentos/avaliacao-layout && npx tsc --noEmit 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/Question.ts` exports `AfirmacaoItem`, `AssociacaoItem`, `AssociacoesContent`, `AssercoesContent`, `TipoLinha` as named interfaces
    - `QuestionContent.afirmacoes` is typed `AfirmacaoItem[]` (not `any[]`)
    - `QuestionContent.associacoes` is typed `AssociacoesContent | null` (not `any`)
    - `QuestionContent.assercoes` is typed `AssercoesContent | null` (not `any`)
    - `Question.tipoLinha` is typed `TipoLinha | null` (not `string | null`)
    - `Reference.instituicao` is typed `string | null` (not `any`)
    - `Reference.totalRegistros` is typed `number | null` (not `any`)
    - `grep ": any" src/domain/Question.ts` returns at most one match (for `content?: any` in `QuestionConstructor`, which is intentionally left)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    `src/domain/Question.ts` has five new exported sub-interfaces; all `any` fields on the rendering path replaced with concrete types; `tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove index signatures and narrow any fields in src/domain/Assessment.ts</name>
  <files>src/domain/Assessment.ts</files>
  <read_first>
    - src/domain/Assessment.ts — read the full current file before any edits (13 lines of interfaces + class); note exact field names at lines 3–43
    - .planning/phases/01-type-foundation/01-PATTERNS.md — lines 222–262: the exact target for `AssessmentLayout` any-field narrowing and index-signature removal; the `Attachment` interface cleanup
    - .planning/phases/01-type-foundation/01-RESEARCH.md — Pitfall 2 (removing index signature may surface compile errors in handlers); Common Pitfalls section; Assumptions Log A2 and A3
    - src/rendering/handlers/ — skim ColumnHandler.ts, HeaderFooterHandler.ts, WatermarkHandler.ts, OrderHandler.ts, TwoColumnsHandler.ts to identify any `assessmentLayout.<fieldname>` accesses that are NOT in the explicit field list; these must be added to the interface or noted as evidence for keeping the field
    - src/rendering/utils/util.ts — check for any `layout.*` field accesses that would surface as errors after removing the index signature
  </read_first>
  <action>
    Modify `src/domain/Assessment.ts` in two stages, running `npx tsc --noEmit` after each stage.

    STAGE A — Narrow `any` fields in `AssessmentLayout` and remove the index signature:

    Replace each `any`-typed field with a concrete type:
    - `instituicao?: any` → `instituicao?: string | null`
    - `ativo?: any` → `ativo?: boolean | null`
    - `rodapeUltimaPagina?: any` → `rodapeUltimaPagina?: string | null`
    - `espacamentoLinhas?: any` → `espacamentoLinhas?: string | null`
    - `mapa?: any` → `mapa?: unknown`
    - `identificado?: any` → `identificado?: unknown`
    - `totalRegistros?: any` → `totalRegistros?: number | null`
    - Remove the `[key: string]: any` index signature line from `AssessmentLayout`

    Run `npx tsc --noEmit` immediately after removing the index signature. If errors appear (e.g., a handler or utility accessing a field not in the explicit list), add that field to `AssessmentLayout` with a concrete type before continuing. Do not re-add the index signature. If a field's type cannot be determined from the codebase, type it as `unknown` (not `any`).

    STAGE B — Clean up `Attachment` interface:
    - Remove `[key: string]: any` from the outer `Attachment` interface
    - Remove `[key: string]: any` from the inner `anexo?: { texto?: string; [key: string]: any }` inline object (keep only `texto?: string`)

    Run `npx tsc --noEmit` after Stage B. The compiler must exit 0.

    Do not change the `Assessment` class constructor or any runtime code — only interface declarations change.
  </action>
  <verify>
    <automated>cd /home/marcelino/Documentos/avaliacao-layout && npx tsc --noEmit 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\[key: string\]" src/domain/Assessment.ts` returns zero matches
    - `grep ": any" src/domain/Assessment.ts` returns zero matches
    - `AssessmentLayout` still contains all previously present named fields (none removed, only types narrowed and index signature removed)
    - `Attachment.anexo` is typed `{ texto?: string } | undefined` (no index signature on the inner object)
    - `npx tsc --noEmit` exits 0
    - The `Assessment` class constructor body is unchanged (no runtime code modified)
  </acceptance_criteria>
  <done>
    `src/domain/Assessment.ts` has no `any` fields and no index signatures; `tsc --noEmit` exits 0; `Assessment` class constructor unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template field access → domain entity | Handlebars templates access `question.visualizaQuestaoParsed.<field>`; the interface must cover every field the template reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/domain/Question.ts` interface narrowing | accept | Pure type change; no runtime behavior; TypeScript compiler validates correctness |
| T-02-02 | Tampering | `src/domain/Assessment.ts` index signature removal | mitigate | Run `tsc --noEmit` after each stage; if new errors surface in handlers, add the missing field explicitly rather than re-adding index signature |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — package legitimacy gate not triggered |
</threat_model>

<verification>
After both tasks complete:

1. `grep -r ": any" src/domain/Question.ts` returns at most one match (`content?: any` in `QuestionConstructor`)
2. `grep ": any" src/domain/Assessment.ts` returns zero matches
3. `grep "\[key: string\]" src/domain/Assessment.ts` returns zero matches
4. `grep -c "export interface" src/domain/Question.ts` returns at least 8 (Reference, AfirmacaoItem, AssociacaoItem, AssociacoesContent, AssercoesContent, TipoLinha, QuestionContent, QuestionConstructor)
5. `npx tsc --noEmit` exits 0
6. `npm run build` succeeds (all three bundle targets unchanged)
</verification>

<success_criteria>
- Domain entity `Question` exposes no `any` on fields accessed during rendering (tipoLinha, afirmacoes, associacoes, assercoes, visualizaQuestaoParsed sub-fields)
- Domain entity `Assessment` exposes no `any` and no index signature on `AssessmentLayout` or `Attachment`
- `QuestionContent` covers all eight template-rendered fields with concrete types
- `tsc --noEmit` exits 0 — no type errors introduced
- `npm run build` succeeds — no bundle breakage
</success_criteria>

<output>
Create `.planning/phases/01-type-foundation/01-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Modified files:
- `src/domain/Question.ts` — adds five new exported interfaces; narrows all rendering-path `any` fields; corrects `tipoLinha` type

Modified files:
- `src/domain/Assessment.ts` — removes `[key: string]: any` from `AssessmentLayout` and `Attachment`; narrows all `any` fields to concrete types

New exported symbols from `src/domain/Question.ts`:
- `AfirmacaoItem` (interface)
- `AssociacaoItem` (interface)
- `AssociacoesContent` (interface)
- `AssercoesContent` (interface)
- `TipoLinha` (interface)
</output>
